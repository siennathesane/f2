apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: functions-claim0
  name: functions-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
