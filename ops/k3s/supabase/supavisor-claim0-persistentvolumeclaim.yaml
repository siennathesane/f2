apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: supavisor-claim0
  name: supavisor-claim0
spec:
  accessModes:
    - ReadOnlyMany
  resources:
    requests:
      storage: 100Mi
